# Dependency Review Action - DISABLED
#
# Workflow ini DISABLED karena trigger error pada push events.
# Untuk mengaktifkan kembali, uncomment bagian 'on:' dibawah
#
# Source repository: https://github.com/actions/dependency-review-action
# Public documentation: https://docs.github.com/en/code-security/supply-chain-security/understanding-your-software-supply-chain/about-dependency-review#dependency-review-enforcement
name: 'Dependency review'

# DISABLED - Uncomment untuk mengaktifkan kembali
# on:
#   pull_request:
#     branches: [ "master", "dev" ]

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: 'Workflow Disabled'
        run: echo "Dependency Review workflow disabled due to recurring errors. Re-enable in workflow file when ready."
